# Multiply-Big-Numbers

This code demonstrates how to multiply integers that are too large to fit in a single int variable. The algorithms accept strings as input and outputs a string, where each character in the strings is a digit. There are two algorithms included here: Standard, which is the normal method of multiplying numbers by hand, and Karatsuba, which uses a faster divide and conquer approach.
